name: Deploy to Google Cloud Run

on:
  push:
    branches:
      - google-cloud

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Authenticate with Google Cloud
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      # Step 3: Set up Google Cloud SDK
      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v1
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      # Step 4: Install Docker Compose
      - name: Install Docker Compose
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/download/v2.20.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose
          docker-compose --version

      # Step 5: Authenticate Docker
      - name: Authenticate Docker
        run: gcloud auth configure-docker europe-west3-docker.pkg.dev

      # Step 6: Build and Push Angular App
      - name: Build and Push Angular App
        working-directory: ./API/SimpleBilling
        run: |
          docker-compose build --no-cache angular-app
          docker tag angular-app:latest europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/multi-container-repo/angular-app:latest
          docker push europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/multi-container-repo/angular-app:latest

      # Step 7: Build and Push Backend App
      - name: Build and Push Backend App
        working-directory: ./API/SimpleBilling
        run: |
          docker-compose build --no-cache simplebilling
          docker tag simplebilling:latest europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/multi-container-repo/simplebilling:latest
          docker push europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/multi-container-repo/simplebilling:latest

      # Step 8: Deploy Angular App to Cloud Run
      - name: Deploy Angular App
        run: |
          gcloud run deploy angular-app \
            --image europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/multi-container-repo/angular-app:latest \
            --platform managed \
            --region europe-west3 \
            --allow-unauthenticated \
            --port 8080 \
            --timeout 300s

      # Step 9: Deploy Backend App to Cloud Run
      - name: Deploy Backend App
        run: |
          gcloud run deploy simplebilling-backend \
            --image europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/multi-container-repo/simplebilling:latest \
            --platform managed \
            --region europe-west3 \
            --allow-unauthenticated \
            --set-env-vars ConnectionStrings__BillingDB='Server=${{ secrets.CLOUD_SQL_CONNECTION_STRING }};Database=BillingDB;User Id=sa;Password=${{ secrets.DB_PASSWORD }}'

      # Step 10: Configure Database in Cloud SQL (Optional Step)
      - name: Deploy Database (Recommended: Use Cloud SQL)
        run: |
          echo "Use a managed database service like Cloud SQL for production workloads."
