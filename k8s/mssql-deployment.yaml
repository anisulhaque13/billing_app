apiVersion: v1
kind: Service
metadata:
  name: mssql-service
spec:
  type: ClusterIP  # Use ClusterIP to allow internal communication
  selector:
    app: mssql
  ports:
    - protocol: TCP
      port: 1433
      targetPort: 1433
